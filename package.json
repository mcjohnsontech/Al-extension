{
  "name": "extension-work",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && cross-env VITE_BUILD_TARGET=content vite build",
    "build:main": "vite build",
    "build:content": "cross-env VITE_BUILD_TARGET=content vite build",
    "preview": "vite preview",
    "server": "node server/index.js",
    "server:dev": "node --watch server/index.js"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "vite": "^8.0.10"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
